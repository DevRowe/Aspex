[package]
name = "aspex-desktop"
version = "0.1.0"
description = "Aspex desktop shell"
authors = ["BroCorp"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
