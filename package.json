{
  "name": "aspex",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "typecheck": "bun run --filter '*' typecheck",
    "test": "bun test",
    "build:xr-lab": "bun run --cwd apps/xr-lab build",
    "test:xr-lab": "bun test apps/xr-lab/src",
    "lint": "biome check .",
    "format": "biome format --write ."
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "@biomejs/biome": "^1.9.0",
    "bun-types": "latest",
    "typescript": "^5.5.0"
  }
}
